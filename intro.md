# 知令（ZHILING）软件使用说明

> **分章节文档（推荐）**：面向用户的完整说明已纳入 **`zhilingsite` 仓库的 `doc/`**：站点 **`/doc/`** 由 **`doc/index.md`**（首页）与 **`doc/.info.md`**（侧栏目录）驱动；桌面端专题为 **`桌面端*.md`** 系列页面。下文为便于随客户端仓库分发的**单文件汇总**；与官网不一致时以线上文档为准。

本文档面向最终用户，说明知令桌面端的主要功能、典型操作流程与常用设置。知令是一款基于 **Electron + React** 的本地桌面应用，面向科研与工程场景：可用自然语言与内置 AI 协作完成文档与代码工作，并支持本地或远程（SSH）项目、计算任务提交与状态跟踪、终端与多类编辑器等。

> **版本提示**：安装包主版本号以 `back_frame/package.json` 中的 `version` 为准；前端界面捆绑版本见同文件 `uiBundleVersion`。根目录 `README.md` 中的版本号可能未及时同步，请以实际安装程序为准。

---

## 一、运行环境与安装

### 1.1 系统与依赖

- **操作系统**：以 Windows 为主（开发与打包说明见项目文档）；部分能力（如应用内更新）在 Linux 客户端可能受限，请以界面提示为准。
- **运行方式**：通过官方安装包安装后启动「知令」桌面应用；开发者亦可本地用 Node.js 18+ 与 **yarn** 分别安装 `front_ui`、`back_frame` 依赖后联调（详见根目录 `README.md`）。

### 1.2 首次启动建议

1. 打开应用后，按界面提示完成**账号登录**（订阅与云端 API 等能力与账号相关，具体以设置页「账户」为准）。
2. 在**设置**中确认界面语言、主题、默认模型等是否符合习惯（见下文「设置」章节）。
3. **打开或新建项目**（本地文件夹或远程服务器目录），再开始使用文件树、编辑器和智能体。

---

## 二、界面总览

### 2.1 主布局

进入已初始化的项目后，主界面大致分为：

- **中间主内容区**：多标签视图（View），可打开多个文件、任务视图、设置页等。
- **一侧为菜单/文件侧栏（MenuPanel）**：通过竖条图标切换「文件」「搜索」等；宽度可拖拽调整，过窄可收起。
- **另一侧为 AI 智能体面板（AgentPanel）**：会话列表、对话区、输入框；与中间编辑区可通过布局选项调整左右位置（如「智能体优先」时智能体与菜单栏会交换左右顺序）。

未打开项目时，中间区域可能显示**欢迎页**，用于打开本地项目、连接远程项目或从最近列表进入。

### 2.2 标题栏与全局入口

- **设置**：通过标题栏菜单或账户区域进入，集中管理账户、常规、编辑器、智能体、安全、通知、存储与颜色个性化等。
- **消息与进度**：全局消息提示、长时间操作的进度指示可在界面边缘或标题栏附近查看。
- **版本更新**：若有新版本，可能通过对话框提示（Windows/macOS 的热更新与全量更新机制见 `back_frame/README.md` 运维说明；Linux 可能需官网下载）。

### 2.3 快捷键提示

- **命令面板**：`Ctrl+Shift+P`（类似 VS Code），可快速执行命令、打开文件、切换最近视图等（具体条目以面板内列表为准）。

---

## 三、项目管理

### 3.1 本地项目

1. 在欢迎页选择打开本地文件夹，或通过系统文件夹选择对话框指定工程根目录。
2. 打开后，**文件树**以该根目录为起点展示；可新建、重命名、删除文件（权限受本机与远程策略限制）。

**Windows 资源管理器集成**（可选）：在设置「常规」中可为文件夹与文件注册「用知令打开」右键菜单（Windows 11 若主菜单未显示，可在「显示更多选项」的经典菜单中查看）。

### 3.2 远程项目（SSH）

1. 在欢迎页选择远程项目入口，按提示配置服务器地址与认证方式（密码或密钥等，以界面为准）。
2. 连接成功后，文件树与编辑器操作的是**远程文件系统**；智能体与任务执行也会基于当前项目的本地/远程上下文。

**注意**：远程项目会建立 SSH 相关连接；若集群对并发会话数有限制，可参考 `back_frame/README.md` 中关于连接池与 `CONN_POOL_SIZE` 的说明，必要时联系管理员调参。

### 3.3 最近项目

欢迎页可列出最近打开过的项目；可一键重新连接，也可从列表中移除某项（仅移除记录，不删除服务器或磁盘上的目录）。

---

## 四、文件、搜索与编辑

### 4.1 文件树（Files）

侧栏「文件」图标展开后显示当前项目的目录树。常见操作包括：

- 单击打开文件到主区新标签或已有视图。
- 上下文菜单中可能包含：**在资源管理器中显示**、**使用系统默认程序打开**、以及 **PDF 转 Markdown（云端 MinerU）** 等批量或单文件转换入口（需登录与相应服务额度，失败时界面会提示，例如账户余额不足时可从提示跳转账户设置）。

### 4.2 搜索（Search）

在侧栏切换到搜索，可在项目范围内检索内容（具体索引范围与性能取决于项目大小与本地/远程；若搜索异常，请确认网络、SSH 与 `window.BASE_URL` 等运行环境，开发者可参考 `.zhiling/zhiling.md`）。

### 4.3 支持的文件与编辑器类型

根据文件类型，知令会选用不同视图，包括但不限于：

- **代码/文本**：Monaco 编辑器（语法高亮、小地图、Vim 模式等可在设置「编辑器」中配置）。
- **Markdown**：预览、Mermaid 流程图等。
- **富文本**：Slate / Lexical 等路线（与项目内 `richDocumentConstants` 等约定一致）。
- **Word（DOCX）**：专用 Docx 视图与转换能力。
- **PDF**：查看；并可通过缓存目录生成标准/高质量 Markdown 衍生文件供智能体阅读（参见智能体系统提示中关于 `.md-cache/` 的说明）。
- **表格等**：按扩展名打开相应视图。

**自动保存与关闭确认**：可在设置「常规」中调整自动保存、关闭标签前是否确认等。

### 4.4 PDF 高质量转 Markdown（MinerU）

对 PDF 使用上下文菜单中的云端转换后，衍生结果通常位于同目录下的 **`.md-cache/`** 中（例如标准版 `*.std.md` 与高质量 `*.high.md`）。若已存在高质量文件，再次转换时界面可能询问是否跳过或覆盖，并支持对批量文件应用同一选择。

---

## 五、AI 智能体

### 5.1 会话与模型

- **多会话**：智能体面板左侧（或折叠条）可管理多个会话；会话内容会持久化到本地数据目录（见下文「数据与缓存」）。
- **模型选择**：每个会话可选择不同大模型。默认模型与后台补全/翻译等使用的模型可在设置「智能体」或相关全局项中配置。站点提供的模型列表可能与账号权限同步；亦支持 **BYOK（自带 API）**：在设置中添加自定义 Base URL、Model Id、API Key 等，以扩展可用模型列表。

### 5.2 工作模式（Agent Mode）

内置模式包括但不限于（名称以界面本地化为准）：

| 模式 | 典型用途 |
|------|----------|
| **Agent（智能体）** | 通用任务：读写文件、运行命令、规划与执行结合。 |
| **Ask（问答）** | 偏对话与说明；工具策略偏只读，适合少改文件的场景。 |
| **Plan（计划）** | 侧重输出可执行计划；系统会约束非计划相关的写入行为。 |
| **Research（科研）** | 面向科研写作与资料整理等（与默认「新建会话」继承规则有关，详见代码中 `AgentPanel` 与 `AgentPromptFactory`）。 |
| **Debug（调试）** | 针对问题定位与调试语境。 |
| **Image（图像）** | 子会话等场景下的图像相关模式。 |

用户可在设置中**新增自定义模式**：填写显示名称、说明正文（Markdown，有长度上限）、以及工具策略（只读 / 完整工具）。

### 5.3 流式回复与 Reasoning

模型回答支持流式输出；部分模型会展示推理（reasoning）过程，便于核对长链思考。

### 5.4 工具调用（Tool Calls）

智能体可调用系统提供的工具，例如：

- 读取/写入项目内文件、执行脚本或 Shell 命令（具体以当前策略与模式为准）。
- 查询任务状态、提交或终止远程任务等（与「任务」模块联动）。
- PDF 转高质量 Markdown、Markdown 转 DOCX 等文档工具。

界面上会以专用组件展示每次工具调用的参数与结果；部分工具执行后可能会**自动在主区打开**相关文件以便核对。

### 5.5 技能包（Skill Package）

侧栏提供「技能包」入口（书签图标），用于浏览与管理智能体可用的技能/规则包；支持在对话框内刷新加载。

### 5.6 语音输入（若已开启）

设置中可配置是否使用单独语音输入对话框、单次最长录音时长等；关闭单独对话框时，识别结果会插入到开始录音时的光标位置。

### 5.7 布局偏好

设置中可切换 **「智能体优先」** 等面板偏好，以交换智能体面板与菜单侧栏的左右位置；仅改变视觉顺序，避免卸载导致会话丢失。

---

## 六、任务（.task）与计算作业

### 6.1 任务文件

扩展名为 **`.task`** 的文件由专用**任务视图**打开，用于配置并提交计算作业。任务通常包含：

- **脚本或命令主体**（具体语法以界面与模板为准）。
- **任务模式**：如 **SLURM**（集群调度）或 **Shell**（直接 Shell 执行）等。
- **参数面板**：队列、资源、时间等（队列设置等标签以界面文案为准）；部分参数仅在允许提交的状态下可改。

任务配置可保存在与 `.task` 同目录的隐藏配置文件 **`.xxx.task.config`**（由 `xxx.task` 派生命名），便于下次加载。

### 6.2 提交与状态

- 在任务视图中提交后，可通过界面查看排队、运行、结束等状态；支持轮询刷新（内部有状态检查器）。
- 任务输出日志可能包含 **Plotly** 等数据文件，任务视图内提供绘图区域展示（见 `TaskView/plots` 相关能力）。

### 6.3 与智能体协作

智能体侧注册了与任务相关的函数（如提交任务、查询状态、终止任务等），可在对话中通过自然语言驱动（仍需符合集群策略与权限）。

---

## 七、终端

- **本地终端**：基于 PTY，可在项目内打开多个终端标签。
- **远程终端**：通过 SSH 在远程会话中执行（与远程项目共用连接体系）。

**设置**：可在「常规」中设置进入项目时是否自动启动终端、以及 Windows 下本地终端默认使用 PowerShell、cmd 或已安装的 Bash（Git/MSYS 等）。修改 Shell 相关选项后，建议**关闭并重新打开**终端标签使环境生效。

---

## 八、数据库与知识库（能力说明）

产品架构支持 **PostgreSQL、MongoDB** 连接与查询，以及**知识库**浏览与管理（前端模块在 `components/Database`、`components/Knowledge`）。当前发行版界面中相关侧栏入口可能默认隐藏或以其他方式开放，若您使用的版本提供对应菜单，操作方式与普通视图类似：在侧栏切换后按向导连接数据源或浏览知识库。

---

## 九、可视化与其它

- **图表**：任务日志或部分视图支持 **Plotly**；Markdown 中可使用 **Mermaid**、**D3** 等（以实际渲染组件为准）。
- **子服务**：Electron 主进程可管理如 Xming、Jupyter 等子服务（见 `back_frame` 文档，最终用户以产品内说明为准）。

---

## 十、设置项导读

设置页左侧为分类，右侧为详细选项：

| 分类 | 主要内容 |
|------|----------|
| **账户** | 登录、订阅、API 用量或与账户相关的服务（如 MinerU 余额提示）。 |
| **常规** | 主题、界面语言、字体大小、自动保存、关闭确认、终端自启、Windows Shell 与右键菜单等。 |
| **编辑器** | 制表符、换行、小地图、行号、Vim 模式等 Monaco 相关选项。 |
| **智能体** | 默认模型、后台模型、自定义工作模式、BYOK 模型列表、命令执行前确认、变更审阅等行为。 |
| **扩展** | 预留；未来版本可能提供扩展管理。 |
| **安全** | 安全与隐私相关选项（以界面为准）。 |
| **通知** | 提示音开关、智能体/任务完成等场景的结束提示音与音量（支持自定义音效文件，有大小限制）。 |
| **存储** | 预留；未来版本可能提供存储管理。 |
| **颜色个性化** | 界面颜色、主题槽位、字号等外观微调。 |

日志级别等开发者选项若出现在设置中，可能受环境变量锁定，请以界面提示为准。

---

## 十一、数据、缓存与隐私提示

- 项目下的 **`.zhiling/`** 目录（若存在）常用于存放会话、计划、临时文件等运行时数据；**请勿手动删除正在使用的会话文件**，除非您清楚影响。
- PDF/DOCX 衍生 Markdown 等可能位于 **`.md-cache/`**。
- 智能体会话索引与存储路径与 `AgentManager`、`SessionsStorageMixin` 等实现相关；重装或清理前请自行备份重要会话与项目文件。

---

## 十二、常见问题（排错思路）

1. **无法登录或 API 报错**：检查网络、账户状态与设置中的模型/BYOK 配置；MinerU 相关报错可关注 402 类余额提示并打开账户设置。
2. **远程连接失败**：核对地址、端口、密钥与防火墙；从欢迎页或重试对话框再次连接；集群侧检查 `MaxSessions` 与会话数。
3. **搜索或 IPC 异常**：确认是否使用官方打包客户端、开发模式下 `window.BASE_URL` 是否与文档一致。
4. **终端命令环境不对**：在设置中修改 Windows Shell 后**重新打开**终端标签。
5. **热更新/升级失败**：查看安装目录 `resources` 下 `hot_update_apply.log`（Windows）或对应脚本日志；必要时从官网下载全量安装包覆盖安装。

---

## 十三、获取帮助与版权

- **开发者文档**：根目录 `README.md`、`AGENTS.md`、`back_frame/README.md`、`front_ui/README.md`。
- **版权**：Copyright © 2024 西安禾成讴沃信息科技有限公司（以产品内声明为准）。

---

*文档根据当前代码仓库功能整理，具体菜单与文案以您安装的版本界面为准。*
